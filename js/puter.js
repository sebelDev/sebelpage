// An object that takes a canvas and turns it into an amazing lil computer game!
export class ComputerGame {
    constructor(canvas) {
        this.canvas = canvas
        this.ctx = canvas.getContext("2d")
    }

    // Loads the stuff that needs to be loaded
    async init() {

    }

    // Runs every frame
    update(time) {
        
    }
}